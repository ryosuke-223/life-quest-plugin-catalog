# プラグイン追加ガイド

Life Questの公式カタログへプラグインを追加する手順です。プラグインは、アプリ本体に実装された固定判定エンジンが解釈するデータパックです。Swift、JavaScript、外部APIを実行するコードは登録できません。

## 追加手順

1. `plugins/<plugin-id>.json` を追加します。
2. `id`、項目ID、グループID、実績IDが重複しないことを確認します。
3. 位置情報を使う項目には、出典が確認できる緯度・経度を指定します。
4. 写真位置で自動判定する項目には `automation.type` を `photoLocation` にします。
5. HealthKitの年間歩数で自動判定する項目には `healthKitAnnualStepCount` と `minimum` を指定します。
6. 必要に応じて `visualizations` に固定ビューを指定します。プラグインからSwiftUIや実行コードを配布することはできません。
7. ローカルで検証します。

```bash
python3 scripts/validate_catalog.py
python3 scripts/build_catalog.py
git diff --check
```

8. `catalog.json` の再生成結果を含めてPRを作成します。

## PR本文に必ず書くこと

- プラグインの目的と対象ユーザー
- 項目データの出典URL
- データのライセンスと再配布可否
- 位置情報・営業状態・路線情報などの更新日
- 既存プラグインの更新の場合は、変更点とバージョンを上げた理由
- 自動判定がある場合の誤判定リスク

出典不明のスクレイピングデータ、再配布が許可されていない施設一覧、利用規約に反する転載データは登録しないでください。

可視化の年別集計は項目の `firstVisitYear` を使います。訪問履歴を持たないプラグインでは、年別値を複数回訪問の回数として説明しないでください。

## 自動判定の注意

`photoLocation` は写真の撮影位置と項目座標を照合します。写真に位置情報がない場合、またはGPSの誤差が大きい場合は自動判定できません。

`healthKitAnnualStepCount` は現在年の歩数合計だけを使います。プラグインからHealthKitの任意のデータ種別を読むことはできません。
