# プラグイン追加ガイド

Life Questの公式カタログへプラグインを追加する手順です。プラグインは、アプリ本体に実装された固定判定エンジンが解釈するデータパックです。Swift、JavaScript、外部APIを実行するコードは登録できません。

## 追加手順

1. `plugins/<plugin-id>.json` を追加します。
2. `id`、項目ID、グループID、実績IDが重複しないことを確認します。
3. 位置情報を使う項目には、出典が確認できる緯度・経度を指定します。複数の離れた地点を持つ項目は `locations` 配列を使い、地点ごとに一意なID、構成資産名、座標を記録します。出典はプラグインの説明資料とPR本文に記録し、マニフェストにはURLを含めません。
4. 写真位置で自動判定する項目には `automation.type` を `photoLocation` にします。`locations` のどれか1地点に写真位置が一致すると、親項目を1件だけ訪問済みにします。
5. HealthKitの年間歩数で自動判定する項目には `healthKitAnnualStepCount` と `minimum` を指定します。
6. `mapStyle` に静的SVGと、未訪問・推定・確認済みのマーカー色、範囲の塗り色・線色を指定します。SVGは正の `viewBox` を持つ20,000バイト以下の `svg`、`g`、`path` だけで構成し、変形・スクリプト・外部参照は使わず、パス命令は `M/m`、`L/l`、`H/h`、`V/v`、`C/c`、`Q/q`、`Z/z` に限ります。色は `#RRGGBB` または `#RRGGBBAA` です。グループの `mapMarkerSVG` はそのグループだけ形状を上書きします。表示設定を指定しない場合は標準の無地ピンになります。
7. 必要に応じて `visualizations` に固定ビューを指定します。プラグインからSwiftUIや実行コードを配布することはできません。
8. ローカルで検証します。

```bash
python3 scripts/validate_catalog.py
python3 scripts/build_catalog.py
git diff --check
```

9. `catalog.json` の再生成結果を含めてPRを作成します。

## PR本文に必ず書くこと

- プラグインの目的と対象ユーザー
- 項目データの出典
- データのライセンスと再配布可否
- 位置情報・営業状態・路線情報などの更新日
- 既存プラグインの更新の場合は、変更点とバージョンを上げた理由
- 自動判定がある場合の誤判定リスク

出典不明のスクレイピングデータ、再配布が許可されていない施設一覧、利用規約に反する転載データは登録しないでください。

可視化の年別集計は項目の `firstVisitYear` を使います。訪問履歴を持たないプラグインでは、年別値を複数回訪問の回数として説明しないでください。

## 自動判定の注意

`photoLocation` は写真の撮影位置と項目座標を照合します。写真に位置情報がない場合、またはGPSの誤差が大きい場合は自動判定できません。

複数地点には地点別の `radiusMeters` を指定できます。既定半径と異なる値は地物の広さや近接する別資産との混同を踏まえて選び、理由を出典資料に記録してください。OSM由来の位置データを配布する場合は、[OSMの帰属表示・ODbL条件](https://www.openstreetmap.org/copyright)に従ってください。

`healthKitAnnualStepCount` は現在年の歩数合計だけを使います。プラグインからHealthKitの任意のデータ種別を読むことはできません。
